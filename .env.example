# Optional local environment for ad hoc runs.
# Copy to .env or export the values in your shell. Do not commit real values.

# SSH_PROXY_BENCH_TARGETS=ssh-only-peer,direct-peer
# SSH_PROXY_BENCH_UPSTREAM_PROXY=http://127.0.0.1:<local-proxy-port>
# SSH_PROXY_BENCH_URL=https://example.com/payload.bin
# SSH_PROXY_BENCH_READINESS_URL=https://example.com/
